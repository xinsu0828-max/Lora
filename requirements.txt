# ============================================================
# 本地开发依赖 (Local / Cursor, Windows, CPU 即可安装)
# 用途: 写代码、调试逻辑、处理数据集与配置。
# 不在本地做真正的 GPU 训练, 所以不包含 unsloth / bitsandbytes。
# 安装: pip install -r requirements.txt
# ============================================================

# --- 核心 HuggingFace 生态 ---
transformers>=4.46.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
accelerate>=1.0.0

# --- PyTorch (本地 CPU 版即可, 仅用于跑通逻辑) ---
torch>=2.3.0

# --- 工具 ---
pyyaml>=6.0          # 读取 configs/ 下的 yaml 配置
sentencepiece>=0.2.0 # 部分 tokenizer 依赖
